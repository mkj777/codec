<Window
    x:Class="Codec.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Codec"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="AppTitleBar" Height="48">
            <TextBlock Text="Codec"
                       FontFamily="/Assets/Sansation-Bold.ttf#Sansation"
                       FontSize="18"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="20,0"/>
        </Grid>

        <GridView Grid.Row="1"
                  x:Name="GameGridView"
                  Margin="10,0,10,10"
                  ItemsSource="{x:Bind Games, Mode=OneWay}"
                  IsItemClickEnabled="False"
                  CanReorderItems="False">

            <!-- This style targets the container of each card and disables the focus border -->
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="UseSystemFocusVisuals" Value="False"/>
                </Style>
            </GridView.ItemContainerStyle>

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:Game" >
                    <Grid x:Name="CardContainer"
                          Width="250" Height="300"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8" >
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="250" />
                                <RowDefinition Height="50" />
                            </Grid.RowDefinitions>

                            <Image Grid.Row="0" Source="{x:Bind Cover, Mode=OneWay}" Stretch="UniformToFill" />

                            <Grid Grid.Row="1" Padding="12,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{x:Bind Name, Mode=OneWay}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>

                                <Button Grid.Column="1"
                                        Content="Play"
                                        Margin="8,0,4,0"
                                        Click="PlayGame_Click"
                                        DataContext="{x:Bind}"
                                        UseSystemFocusVisuals="False"/>

                                <Button Grid.Column="2"
                                        Content="Remove"
                                        Click="RemoveGame_Click"
                                        DataContext="{x:Bind}"
                                        UseSystemFocusVisuals="False"/>
                            </Grid>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <Button Grid.Row="1"
                x:Name="AddGameButton"
                Content="Add Game"
                Click="AddGame_Click"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="20"
                FontWeight="SemiBold"
                Style="{StaticResource DefaultButtonStyle}"
                UseSystemFocusVisuals="False"/>
    </Grid>
</Window>

